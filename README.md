# PrinterServer

Simple Express server with an example endpoint.

## Install

```bash
npm install
```

## Run the server

```bash
npm start
```

The server will start on `http://localhost:3000`.

## Example endpoints

- `GET /` – returns a simple text greeting.
- `GET /status` – returns JSON with a status message and timestamp.

